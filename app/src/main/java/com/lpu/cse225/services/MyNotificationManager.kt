package com.lpu.cse225.services

import android.app.Notification
import android.app.NotificationManager
import android.content.Context
import androidx.core.content.ContextCompat

class MyNotificationManager {
    fun showNotification(context: Context, title: String, body: String, payload: Map<String, Any>) {
//        val notificationManager: NotificationManager = ContextCompat.getSystemService(context, No) as NotificationManager
    }
}